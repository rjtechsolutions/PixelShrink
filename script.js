const $=x=>document.getElementById(x);let file=null,blob=null,url=null,origUrl=null,timer;const drop=$('drop'),app=$('app'),input=$('file');const bytes=n=>{if(!n)return'0 B';let u=['B','KB','MB','GB'],i=Math.floor(Math.log(n)/Math.log(1024));return (n/Math.pow(1024,i)).toFixed(i?2:0)+' '+u[i]};function load(f){if(!f||!f.type.startsWith('image/'))return;file=f;origUrl=URL.createObjectURL(f);$('orig').src=origUrl;$('os').textContent=bytes(f.size);let im=new Image();im.onload=()=>{$('dim').textContent=im.width+' × '+im.height+'px'};im.src=origUrl;drop.hidden=true;app.hidden=false;compress()}function compress(){if(!file)return;let im=new Image();im.onload=()=>{let c=document.createElement('canvas');c.width=im.naturalWidth;c.height=im.naturalHeight;let x=c.getContext('2d');if($('fmt').value==='image/jpeg'){x.fillStyle='#fff';x.fillRect(0,0,c.width,c.height)}x.drawImage(im,0,0);c.toBlob(b=>{if(!b)return;blob=b;if(url)URL.revokeObjectURL(url);url=URL.createObjectURL(b);$('comp').src=url;$('cs').textContent=bytes(b.size);$('save').textContent=Math.max(0,(1-b.size/file.size)*100).toFixed(1)+'% saved';$('result').textContent=bytes(file.size)+' → '+bytes(b.size)},$('fmt').value,+$('q').value/100)};im.src=origUrl}function queue(){clearTimeout(timer);timer=setTimeout(compress,150)}$('browse').onclick=e=>{e.stopPropagation();input.click()};drop.onclick=()=>input.click();input.onchange=()=>load(input.files[0]);drop.ondragover=e=>{e.preventDefault();drop.style.borderColor='#00e6c3'};drop.ondrop=e=>{e.preventDefault();load(e.dataTransfer.files[0])};$('q').oninput=()=>{$('qv').textContent=$('q').value+'%';queue()};$('fmt').onchange=queue;$('download').onclick=()=>{if(!blob)return;let ext=$('fmt').value==='image/webp'?'webp':$('fmt').value==='image/jpeg'?'jpg':'png',a=document.createElement('a');a.href=url;a.download=file.name.replace(/\.[^.]+$/,'')+'-compressed.'+ext;a.click()};$('change').onclick=()=>input.click();$('reset').onclick=()=>location.reload();
